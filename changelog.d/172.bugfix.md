Parakeet works again in the installed Windows app. Choosing it used to fail
immediately — the installer left out a file the speech engine reads about
itself on startup — so dictation fell back to Whisper.
