Linux builds ship as a tarball and an AppImage (Ubuntu 24.04 or newer, any rolling distro), with an AUR `cadent-bin` recipe; Cadent registers its desktop entry and icon on first run and can start at login.
