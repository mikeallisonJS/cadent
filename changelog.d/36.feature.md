On Linux Wayland, dictation pastes first: through the compositor's clipboard protocol where it has one (KDE, wlroots) and the Clipboard portal on GNOME; where neither exists Cadent types instead and says so in App overrides.
